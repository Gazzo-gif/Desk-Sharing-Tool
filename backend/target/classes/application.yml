spring:
  datasource:
    url: jdbc:mariadb://database:3306/mydatabase
    username: root
    password: mypass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
